#include <SDL3/SDL.h>
// #include <SDL3/SDL_main.h>
#include <app.h>
#include <array>

int main(int argc, char *argv[]) {
    Application app;
    app.start(argc, argv);
    // gui_test();
    return 0;
}